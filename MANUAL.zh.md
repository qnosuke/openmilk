[日本語](./MANUAL.md) | [English](./MANUAL.en.md) | **中文**

# openmilk 使用手册（新手向）

openmilk 是一款完全在浏览器中运行的任务管理应用。无需注册、无需安装、没有服务器——
所有数据都保存在你自己的浏览器里（IndexedDB），不会发送到任何地方。

本手册面向第一次使用的用户。可以按标题当索引来查。

---

## 目录

1. [上手五分钟](#上手五分钟)
2. [添加任务（快速添加）](#添加任务快速添加)
3. [完成、推迟、删除](#完成推迟删除)
4. [编辑详情](#编辑详情)
5. [INBOX 与分拣模式](#inbox-与分拣模式)
6. [管理列表](#管理列表)
7. [查找任务（搜索与筛选）](#查找任务搜索与筛选)
8. [排序](#排序)
9. [计时与番茄钟](#计时与番茄钟)
10. [统计](#统计)
11. [垃圾箱](#垃圾箱)
12. [备份与恢复](#备份与恢复)
13. [自己的数据自己保管](#自己的数据自己保管)
14. [常见问题](#常见问题)

---

## 上手五分钟

1. **添加一条任务** — 在顶部输入框输入 `明天交报告 30分钟 !2 #工作`，按回车。
   截止日期、预估用时、优先级、标签都会从这一行里自动解析出来。
2. **完成任务** — 勾选任务行左侧的复选框，列表上方会出现操作栏，点击「完成 {n} 件」。
3. **编辑内容** — 点击任务名打开编辑对话框（备注、截止日期、时间、提醒、优先级、
   列表、预估、标签）。
4. **查找** — 在左侧搜索框输入关键词，匹配部分会高亮显示。
5. **备份** — 点击左下角 ⚙（牛奶图标）→「导出」，保存一个 JSON 文件。

就这些。其余内容按需阅读。

> **切换语言**: ⚙ → 最下方的「语言」可选 日本語 / English / 中文。
> 界面语言和输入语言互不影响——界面是中文时，也可以用日语或英语的快速添加语法。

---

## 添加任务（快速添加）

在顶部输入框输入一行，按回车。不加空格也能从词中解析，日 / 英 / 中输入都支持：

```
明天交报告 30分钟 !2 #工作
```

| 内容 | 写法示例 |
|---|---|
| 截止日期 | `今天` `明天` `后天` `周三` `下周一` `9/15` `9月15日` `2026-10-01` |
| 截止日期（日/英） | `明日` `金曜` / `today` `tomorrow` `next fri` |
| 时间 | `10点` `下午3点` `9:30` `2pm` `10時` `午後3時` |
| 预估用时 | `30分钟` `2小时` `45min` `1h30min` `0.5h` `30分` `2時間` |
| 优先级 | `!1`（最高）`!2` `!3` |
| 标签 | `#工作` `#购物`（可多个） |

- 日期和时间可以同时写，如「明天10点」。只写时间不写日期时按「今天」处理
- 任务会加到**当前显示的列表**中。在「全部」或 INBOX 视图下添加时进入 INBOX
- 解析不了的词会原样保留在标题里，即使解析失败也不会丢失信息
- **多行粘贴**: 把带换行的文本粘贴到输入框（或把 .txt / .md 文件拖到窗口里），
  每行变成一条任务（上限 100 行）。适合把在别处记下的待办清单一次性倒进来

### 通过 URL 一次性添加

打开类似 `https://qnosuke.github.io/openmilk/?add=明天9点开会` 的网址即可直接注册
（10分钟内的相同内容不会重复添加）。适合手机快捷指令和自动化场景。

---

## 完成、推迟、删除

openmilk 的复选框不是「完成按钮」，而是**选择**：

1. 勾选要操作的任务（可多选）
2. 点击列表上方操作栏中的「完成 {n} 件」
3. 「清除选择」可取消所有勾选

同一个操作栏还可以：

- **推迟**: 「推迟 {n} 件」把截止日期推后一天（没有日期的任务变成明天）
- **批量添加 / 删除标签**: 输入标签名后点「+ 标签」或「− 标签」，对选中任务全部生效

删除任务（移入垃圾箱）：点击任务名打开编辑对话框，点「删除」。
误删可以从垃圾箱恢复（见[垃圾箱](#垃圾箱)）。

---

## 编辑详情

点击任务名打开编辑对话框：

- **备注**: 支持多行；列表中只预览第一行
- **截止日期与时间**: 时间选择器按 5 分钟步进，也可以直接输入（如 9:23），原样保存
- **提醒**: 可选截止时间前 5 分钟到 1 天。首次设置时会请求通知权限。
  通知只在浏览器 / PWA 打开期间有效（没有服务器推送）
- **优先级**: `!1` 最高。!1 的任务在列表中会加粗并带红色边线
- **列表**: 把任务移动到其他列表（留空 = INBOX）
- **预估 / 实绩**: 「1小时30分」这类自然输入也能解析
- **子任务（拆分）**: 在下方「子任务」栏里每行输入一条，点「拆分并添加」。
  每行都可用快速添加语法；子任务会继承父任务的列表和标签，
  在父任务下方缩进显示，父任务行上出现「▸ 1/3」这样的进度。
  子任务**只有一层**，不能再对子任务进行拆分

「取消」丢弃修改；「保存」或点击对话框背景保存修改。

---

## INBOX 与分拣模式

INBOX 是**先随手丢进去**、之后再整理的地方。

- 打开 INBOX 会进入**分拣模式**（出现深色横幅，侧边栏 INBOX 按钮颜色反转）
- 每条任务右侧会出现 `next action` / `waiting` / `someday` 按钮，
  点一下即可把任务移动到对应列表
- **也可以用键盘分拣**: `j`/`k`（或`↓`/`↑`）移动光标，`1`/`2`/`3` 移动到
  next action/waiting/someday，`x` 完成，`e` 编辑，`Space` 选择（`Esc` 取消）。
  全局快捷键：`a` 跳到添加框，`/` 跳到搜索框
- INBOX 的排序**固定为添加顺序（旧任务在前）**，让你从最早收进来的开始处理
  （其他视图的排序见[排序](#排序)）

先写下来，再慢慢想。清空 INBOX 的爽快感就是设计理念。

---

## 管理列表

- 在左侧边栏切换列表：`全部` / `INBOX` / 固定列表（`next action` / `waiting` /
  `someday`）+ 自己创建的列表
- **新建**: 打开 ⚙，在「新建列表」输入框输入名字。创建后会自动切换到新列表，
  可以马上添加任务
- **删除**: 列表名旁边的 ×。**任务不会丢失，会回到 INBOX**
- 固定列表（next action / waiting / someday）无法删除

---

## 查找任务（搜索与筛选）

- **搜索**: 左侧搜索框覆盖标题、备注和标签，匹配部分会高亮
- **时间范围**: 侧边栏的 2×2 方块（已过期 / 今天 / 明天 / 一周）按截止日期筛选，
  再点一次取消
- **标签筛选**: 点击任务行上的 `#工作` 标签或侧边栏的标签云。顶部出现的「#工作 ✕」
  按钮可取消
- **隐藏标签**: 在 ⚙ 的标签列表里可以静音（⊘）不想看到的标签，
  带该标签的任务会从所有视图中消失（同样在 ⚙ 中解除）

---

## 排序

- INBOX 以外的视图可在右上角切换排序方式：**按截止日期**（早的在前，同日内按时间）/
  **按优先级** / **最新添加**。选择会被记住
- INBOX 固定为添加顺序（见上文）
- 已完成的任务总是排在列表末尾

---

## 计时与番茄钟

- 点击任务行的 **▶** 开始计时，**⏸** 停止。同一时间只能给一条任务计时
  （给另一条点 ▶ 时，上一个计时会自动停止并把用时计入实绩）
- 计时中完成任务时，已流逝的时间会自动计入实绩
- 计时中会实时显示用时，超过预估值会变红（显示为「25m / 30m」这样的对照）

### 番茄钟

计时过程中会自动运行番茄钟周期：

- 顶部出现 🍅 标记，倒计时显示「第1轮 剩24:59」
- 一段专注结束后会通知「休息一下吧」，休息结束后通知「继续专注」
- 每第 4 轮后的休息更长（15 分钟）
- 设置（专注 25 分钟 / 短休息 5 分钟 / 长休息 15 分钟 / 长休息间隔 4 轮）
  在 ⚙ 的「番茄钟」区域修改

> 收不到通知时，请检查浏览器的通知权限（首次设置提醒或番茄钟休息触发时会请求授权）。

---

## 统计

点击顶部的「统计」标签，可以回顾已完成任务的表现：

- **今天 / 本周 / 本月 / 今年** 的完成数量
- 预估用时合计与实绩（计时）合计
- **按计划率**: 带预估的任务中，实绩没有超出预估的比例

目的是让你发现「预估太乐观了」。建议在快速添加时随手带上预估用时。

---

## 垃圾箱

- 被删除的任务会在垃圾箱保留 **30 天**（⚙ →「垃圾箱」开关视图）
- 在垃圾箱中勾选后点「恢复 {n} 件」即可还原
- 超过 30 天的任务会在启动时自动彻底删除

---

## 备份与恢复

⚙ →「导出」可以把全部数据（含已删除）保存为一个 JSON 文件。

### 自动备份（Chrome / Edge）

⚙ →「自动备份」→「选择文件夹」指定一个监视文件夹后:

- 每次修改任务时，都会自动保存到文件夹内的 `openmilk-backup.json`
- 把 `inbox.md` 放进文件夹，应用在启动时、获得焦点时会把每一行导入为任务
  （一行 = 一条任务，`- 买牛奶 明天 !1` 这样的列表写法也可以。
  处理完的文件会移到 `imported/` 子文件夹）
- 放入 `inbox.json`（下面的备份格式）也会同样导入

这也是 AI 代理的接口：代理通过写 `inbox.md` 来添加任务、读 `openmilk-backup.json`
来获取任务列表。数据格式的正本发布在
`openmilk.schema.json`（https://qnosuke.github.io/openmilk/openmilk.schema.json）。

### 导出与导入

- **恢复 / 迁移**: 用「导入」载入 JSON。相同任务以**更新时间较新**的一方为准，
  做幂等合并——无论是恢复到空白状态还是与现有数据合并都是安全的
- **推荐习惯**: 每周导出一次，把文件放进 Dropbox 或 iCloud 云盘
- 要批量清理已完成任务：⚙ →「删除已完成任务」（两步确认）。删除前先导出更保险

---

## 自己的数据自己保管

openmilk 的基本精神是 **own your data（数据由自己掌握）**。

- 数据只存在于你的浏览器（IndexedDB）中，不会发送到云端或服务器
- **清除网站数据会连带删除任务**: 浏览器的「清除站点数据」也会清掉 openmilk，
  请养成定期导出的习惯
- **作为 PWA 使用**: 用 Chrome / Safari 打开 https://qnosuke.github.io/openmilk/
  并选择「安装」，之后可以像应用一样启动，并支持离线使用
  （开发服务器 localhost:5173 不支持离线模式）
- 多设备自动同步尚未实现。要跨设备携带数据，请通过「导出 → 在另一台设备导入」的
  JSON 方式

---

## 常见问题

**Q. 数据保存在哪里？**
只保存在你浏览器的 IndexedDB 里。不会上传到服务器，也不在这个仓库里。

**Q. 手机和电脑能共用数据吗？**
暂不支持自动同步。可以通过导出 / 导入 JSON 手动保持一致。

**Q. 任务行上找不到「完成」按钮**
这是设计如此：先勾选复选框，再用列表上方的「完成 {n} 件」。

**Q. 收不到通知**
通知只在浏览器 / PWA 打开期间有效（没有服务器推送），并且需要系统 / 浏览器的
通知权限。建议把应用安装为 PWA。

**Q. 添加的任务找不到了**
它进入了你当时显示的列表。请在 INBOX 或「全部」中查找，并确认没有标签被静音。

**Q. 不小心重复添加了同一条任务**
`?add=` 链接在 10 分钟内的重复执行会自动跳过。手动输错导致的重复，
可以在编辑对话框中「删除」。

---

设计理念与路线图见 [DEVELOPMENT_FLOW.md](./DEVELOPMENT_FLOW.md)（日语），
开发者与代理说明见 [AGENTS.md](./AGENTS.md)（日语），
简介见 [README](./README.zh.md)。

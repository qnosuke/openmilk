# openmilk 開発の流れ（ロードマップ）

Remember The Milk の代わりとなる、ブラウザ駐在型タスク管理 Web アプリ「openmilk」を
構想から公開まで完成させるまでの全工程をまとめた文書。

- **形態**: サーバーなしの静的 Web アプリ（PWA）— データはブラウザ内 (IndexedDB) に保存
- **スタック**: Vite + Svelte + Dexie.js + PWA（すべて無償・オープンソース）
- **ランニングコスト**: 0 円（GitHub Pages / Cloudflare Pages の無料枠で配信）

## 全体像

基本の 7 フェーズに、AI エージェント対応の拡張フェーズ（6.5）を加えた 8 ステップ構成です。
**各フェーズの終わりには必ず「動くもの」がある**のが原則です。途中で中断しても、
その時点のものがちゃんと使えます。

```
Phase 0  事前準備          （道具をそろえる）
   ↓
Phase 1  プロジェクト骨組み   （画面に Hello World）
   ↓
Phase 2  データ層          （ブラウザ内DBに保存できる）
   ↓
Phase 3  最小のタスクアプリ   ← ここで初めて「自分のアプリ」が成立
   ↓
Phase 4  RTM 的コア機能     （クイック追加・期限・繰り返し・ショートカット）
   ↓
Phase 5  PWA 化           （インストール・オフライン対応）
   ↓
Phase 6  バックアップ機能    （JSON 書き出し・自動バックアップ）
   ↓
Phase 6.5  エージェント対応   （inbox ファイル・JSON Schema・自動化配慮）
   ↓
Phase 7  公開             （無料ホスティングに置いて常に使える状態へ）
```

---

## Phase 0: 事前準備（所要: 約 30 分）

やること:

1. **Node.js (LTS 版)** をインストール — https://nodejs.org
2. ターミナルで `node -v` を実行し、バージョンが表示されることを確認
3. エディタを用意（VS Code など）
4. （任意）公開用に GitHub アカウントを作成 — 開発だけなら不要

完了条件: `node -v` と `npm -v` が実行できる。

## Phase 1: プロジェクトの骨組み（所要: 半日）

やること:

1. このフォルダで Vite プロジェクトを作成:

   ```bash
   npm create vite@latest . -- --template svelte-ts
   npm install
   npm run dev
   ```

2. ブラウザで `http://localhost:5173` を開き、初期画面を確認
3. `git init` して最初のコミット（以後、細かくコミットする習慣にする）
4. ディレクトリ構成を決める（最初に決めておくと迷わない）:

   ```
   src/
     lib/
       db/          ← Dexie 周り（データ保存の窓口=リポジトリ層）
       components/  ← 画面部品（タスク1行、入力フォームなど）
       utils/       ← 日付計算・自然言語パース
     App.svelte     ← 画面の入り口
   ```

完了条件: `npm run dev` で開発サーバーが立ち、画面が見える。

## Phase 2: データ層の実装（所要: 1〜2 日）

設計の要となるフェーズ。**UI はまだ作らない**。

やること:

1. Dexie.js を導入: `npm install dexie`
2. Task スキーマを定義（`schemaVersion` を含めること）:

   ```
   Task {
     id: UUID
     title, notes
     due?: Date            期限
     priority?: 1|2|3      優先度
     tags: string[]        タグ
     recurrence?: string   繰り返しルール
     completedAt?: Date    完了日時
     createdAt, updatedAt  作成・更新日時（将来の同期に備え必須）
     deleted: boolean      論理削除フラグ（削除は物理削除しない）
   }
   ```

3. リポジトリ層 `taskRepository.ts` を作る — タスクの追加・更新・取得・論理削除の
   関数だけを集めたファイル。UI からはこのファイル経由でのみデータを触る
   （将来ストレージを差し替えるときの保険）

完了条件: 開発コンソールや一時的なコードから、タスクの保存→取得→更新→論理削除ができる。

## Phase 3: 最小のタスクアプリ（所要: 数日）★縦断スライス

ここで初めて「見える画面」と「保存されるデータ」がつながり、openmilk が
アプリとして成立します。機能は最低限でよい。

やること:

1. インボックス画面: 未完了タスクの一覧表示
2. タスク追加フォーム（テキストを入れて Enter で追加）
3. チェックで完了、削除ボタンで論理削除
4. リロードしてもタスクが消えないことを確認（IndexedDB に保存されている証拠）

完了条件: 「追加 → 一覧 → 完了 → 削除」の一周が動く。
**この時点で既に RTM の代わりとして最低限使える**ので、一度使い込んでみる。

## Phase 4: RTM 的コア機能（所要: 1〜2 週間・好みで削減可）

RTM の使い勝手の本質を再現するフェーズ。優先順位順に:

| # | 機能 | 解説 |
|---|------|------|
| 1 | 自然言語クイック追加 | 「資料レビュー 明日 !1 #仕事」のように1行で期限・優先度・タグを解釈（RTM Smart Add 相当。最重要） |
| 2 | 期限・優先度・タグの表示と編集 | 今日/期限切れの色分けも |
| 3 | 今日 / 期限順ビュー | インボックスと分けた2画面構成 |
| 4 | キーボードショートカット | j/k 移動、x 完了、d 期限設定など（RTM ユーザーの筋肉） |
| 5 | 繰り返しタスク | 「毎週月曜」「毎月1日」の完了時に次回を自動生成 |
| 6 | 検索 | タイトル・メモ・タグの絞り込み |

完了条件: **実際の自分のタスク管理を 1 週間、openmilk だけで回してみて**耐えられること。
足りない部分だけ追加実装する（ここが一番大事なテスト）。

## Phase 5: PWA 化（✅ 実装済み: 2026-09-02。アイコンは public/milk.png から生成）

やること:

1. ~~`vite-plugin-pwa` を導入し、`vite.config.ts` に設定を追加~~
2. ~~アイコン（192px / 512px）とアプリ名・テーマ色を manifest に設定~~
   （public/icons/ に sharp で生成。再生成は milk.png 差し替え後 `node` で resize）
3. ~~ビルドしてデプロイ先なしでも確認可能な状態にする~~
4. ~~ブラウザ（Chrome 等）から「インストール」してドックに追加~~（本番ビルド `npm run preview` から実施）
5. ~~ネットを切った状態で起動し、オフラインで動くことを確認~~

完了条件: ~~インストールしたアプリがオフラインで起動・動作する~~
（dev サーバー 5173 では Service Worker は無効。インストールは本番ビルドから行う）

## Phase 6: バックアップ機能（所要: 1〜2 日）

要件の核。3 段階で作る:

1. **JSON エクスポート / インポート**（必須）— 全タスクを 1 ファイルに書き出し。
   先頭に `schemaVersion` を持たせ、インポート時に形式の差分を吸収
2. **自動バックアップ**（Chrome / Edge）— File System Access API
   でユーザー指定フォルダ（iCloud Drive や Dropbox 内など）に変更のたび自動保存。
   実質クラウドバックアップ兼、複数 Mac 間の手動同期になる
3. （任意）**Markdown エクスポート** — 人間が読める避難経路

完了条件: エクスポート → アプリデータを全消し → インポート、で完全復元できる。
**バックアップからの復元を実際に一度やって確認するまでが完成**。

## Phase 6.5: エージェント対応（所要: 1〜2 日）

OpenClaw などの AI エージェントが openmilk を操作できるようにするフェーズ。
データ本体の IndexedDB はブラウザのサンドボックス内にあり外部から直接触れないため、
**エージェントの能力（ファイル読み書き・ブラウザ操作）に合わせた入口を用意する**。

### やること

1. **データ形式の正本を公開する**: バックアップ JSON の形式を
   `openmilk.schema.json`（JSON Schema）としてリポジトリに置く。
   エージェントはこれを読んで正確なファイルを生成できる
2. **inbox ファイルの取り込み**: Phase 6 の自動バックアップ先と同じ監視フォルダに
   `inbox.md` または `inbox.json` が置かれたら、アプリ起動時/フォーカス時に取り込む
   - `inbox.md` は Phase 4 の自然言語パーサーを共用する:
     `- 牛乳を買う 明日 !1 #買い物` のような1行を1タスクに変換
   - 取り込みは UUID と `updatedAt` で冪等に: 同じ id があれば新しい方を採用し二重登録しない。
     処理済みファイルは `imported/` フォルダへ移動
3. **アプリ → エージェント方向の確認**: 自動バックアップの JSON をエージェントが
   読むだけで現在のタスク一覧が分かること（実装は Phase 6 の再利用、追記は最小）
4. **クイック追加 URL**: `/?add=資料レビュー 明日 !1 #仕事` のような URL を開くと
   クイック追加フォームに投入済みの状態で起動する入口を作る（ブラウザ自動化での
   ワンショット追加用。ページを1回開くだけで追加が完了する）
5. **UI の自動化配慮チェック**（Phase 3〜4 で作ったものの確認）:
   - 正しい HTML 要素を使う（`<button>`、`<input>`、`<label>` など）
   - アイコンだけのボタンには `aria-label` を付ける
   - 主要操作がキーボードだけで完結する（クリックよりキー入力の方が自動化が安定する）
   - canvas や独自描画を使わない
6. **AGENTS.md を置く**: リポジトリの構成・慣習・テストの実行方法を書いた指示書。
   コードを触るエージェント（ZCode / OpenClaw など）の作業精度が上がる
7. （任意）**OpenClaw 用 skill ファイル**: 「タスク追加は inbox.md に1行書く」
   「今日のタスク一覧はバックアップ JSON を読む」といった手順を、
   エージェント側に読ませる Markdown の skill としてまとめる

完了条件: エージェントに「明日の朝9時にゴミ出しを追加して」と頼めば inbox 経由で
タスクが登録され、「今日のタスクは？」と聞けばバックアップ JSON を読んで答えられること。

### やらないこと

- サーバーを立てて REST API を生やすこと。ファイル連携 + ブラウザ自動化で実用上
  足りており、サーバーは複数デバイス同期とセットで v2 検討に回す

## Phase 7: 公開（✅ 実装済み: 2026-09-06・GitHub Pages）

公開 URL: **https://qnosuke.github.io/openmilk/**
（main への push で Actions が自動ビルド＆デプロイ。`.github/workflows/deploy.yml`）

やること:

1. `npm run build` で本番用ファイルを生成
2. 無料静的ホスティングに置く:
   - **GitHub Pages** — GitHub に push するだけで公開（Actions で自動化）
   - **Cloudflare Pages** — 同様に無料・高速
3. 発行された URL にブラウザでアクセスし、PWA としてインストール

完了条件: 別の Mac やスマホのブラウザから URL でアクセスできる。
以後は「コードを直す → git push → 数十秒後に本番反映」のループになる。

---

## 公開後フィードバック（2026-09-06・実際に使ってみて上がったアイデア）

実際に使ってみて感じたことの記録。優先順位は未定。
（元メモに付いていた `#work` や「2分」などの注記はそのまま残してある）
実装の際は AGENTS.md と `skills/openmilk/SKILL.md` も更新すること。

### 小改善（小さくすぐできそうなもの）

- ~~日本語 UI の期限切れ表記~~ — **修正済み（2026-09-06）**: 「逾期」と表示していたのを
  「期限切れ」へ変更（`src/lib/utils/date.ts` の DUE_LABELS）。「逾期という日本語はない」との指摘
- ~~検索した文字列のハイライト表示~~ — **実装済み（2026-09-06）**: タイトルとメモの
  該当部分を `<mark>` で強調（`src/lib/utils/highlight.ts`）
- ~~INBOX の並び順は追加順で固定~~ — **実装済み（2026-09-06）**: INBOX では
  追加順（古いものが先）で固定し、ソート切り替えを非表示に
- ~~複数行テキストの貼り付け/ドラッグで、改行区切りの 1 行ずつを INBOX に一括追加~~ —
  **実装済み（2026-09-06）**: クイック追加欄への複数行貼り付けと .txt/.md の
  ウィンドウドロップに対応（1行=1タスク、クイック追加の記法が使える、上限100行）
- ~~タグの一括追加・一括削除~~ — **実装済み（2026-09-06）**: 一括バーに
  タグ名入力＋「+タグ / −タグ」ボタン
- ~~INBOX 仕分けモード~~ — **実装済み（2026-09-06）**: INBOX の行に
  next action/waiting/someday へのワンクリック移動ボタン。モード中は黒バナーと
  INBOX ボタンの色反転で表示

### 機能拡張（中規模）

- ~~ポモドーロタイマー~~ — **実装済み（2026-09-06）**: 計測タイマーに重ねて動作。
  集中/休憩/長休憩の長さと長休憩の間隔を ⚙ で設定（`src/lib/utils/pomodoro.ts`）。
  休憩に入るタイミングで通知
- ~~タスク分割~~ — **実装済み（2026-09-06）**: 編集ダイアログの「サブタスク」欄で
  1 行ずつ分割追加（`Task.parentId`・DB v3・バックアップ schemaVersion 4）。
  子は親の下にインデント表示、親に進捗表示。
  **1 階層限定**とする決定（2026-09-07）: 子タスクでは分割欄を非表示にして作成を防ぎ、
  表示側も `splitOneLevel`（`src/lib/utils/subtasks.ts`）で孫を通常行として扱う
- ~~重要度が一目で分かる表示~~ — **実装済み（2026-09-06）**: 優先度 !1 のタスクは
  タイトル太字＋行の左端に赤ライン
- ~~パフォーマンス統計~~ — **実装済み（2026-09-06）**: ヘッダーの「統計」タブで
  日次/週次/月次/年次の完了数・見積もり計・実績計・予定通り率を表示
  （`StatsView.svelte`）

### スキン・テーマ

- ゲーミング風スキン（ユーザー自身が欲しい）
- 将来の構想（すぐには作らない）: 課金スキン、Windows 98 風、ターミナルモード。
  「スキンだけ有料」とする収益モデルの案もある。
  実装の土台は CSS 変数によるテーマ切り替えから始めるのが自然

### エージェント・同期系（大きめ / v2 候補と接続）

- MCP サーバーとして外部から追加できる「エージェント主導の todo アプリ」へ
  （Phase 6.5 の発展形。inbox.md 取り込み・openmilk.schema.json 公開と接続する）
- スマホアプリとのデータ連動（→ 下の v2 候補「複数デバイス間の同期」と同じ課題）
- **「own your data」を基本精神とする**: クラウドに預けるよりデータを自分で管理する。
  機能・同期の設計判断の優先基準として明記する

### 要確認

- ネーミングの変更 — **決定（2026-09-06）**: 製品版になった暁に改名を検討する。現時点では openmilk のまま進める

---

## 公開後（v2 候補・優先順位未定）

- 複数デバイス間の同期（Phase 2 で `updatedAt` を入れてあるので後付け可能。
  CRDT ライブラリ Yjs / Automerge、またはバックアップファイル経由の手動マージから）
- スマートリスト（保存済み検索）
- ローカル REST API モード（エージェントや他ツールからの直接 CRUD。
  Phase 6.5 のファイル連携で不足が出た場合の拡張）
- Markdown / OPFS など追加の書き出し形式
- 見た目の磨き込み（ダークモード等）

## 進め方のコツ

- **常に動くものを保つ**: 各フェーズがそれ自体で完成形。完璧を目指して一気に作らない
- **Phase 4 の「1 週間使ってみる」を省略しない**: 本当に必要な機能は使ってみないと分からない。
  RTM にあった機能でも、自分が使わないものは実装しない判断をしてよい
- **ZCode への任せ方**:
  - 任せられる: 定型的な実装、リファクタリング、テスト作成、エラーの調査
  - 自分で決める: 機能の優先順位、UI の使い心地、キーボード操作の感覚

## 使用する技術のまとめ

| 技術 | 役割 | ライセンス |
|------|------|-----------|
| Vite | 開発サーバー・ビルド工具（土台） | MIT |
| Svelte | UI を作るフレームワーク | MIT |
| Dexie.js | ブラウザ内 DB (IndexedDB) の操作ライブラリ | Apache-2.0 |
| PWA | インストール・オフライン化（ブラウザ標準規格） | — |

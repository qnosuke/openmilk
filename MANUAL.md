[日本語](./MANUAL.md) | [English](./MANUAL.en.md) | [中文](./MANUAL.zh.md)

# openmilk 使い方マニュアル（初めての方へ）

openmilk はブラウザだけで動くタスク管理アプリです。インストールや登録は不要で、
データはすべてお使いのブラウザ内（IndexedDB）に保存されます。サーバーに送信されることはありません。

このマニュアルは初めて使う方向け。辞書的に引きたいときは各セクションの見出しを目印にどうぞ。

---

## 目次

1. [はじめての5分](#はじめての5分)
2. [タスクを追加する（クイック追加）](#タスクを追加するクイック追加)
3. [完了・延期・削除](#完了延期削除)
4. [詳細を編集する](#詳細を編集する)
5. [INBOX と仕分けモード](#inbox-と仕分けモード)
6. [リストを管理する](#リストを管理する)
7. [タスクを探す（検索・絞り込み）](#タスクを探す検索絞り込み)
8. [並び順](#並び順)
9. [計測タイマーとポモドーロ](#計測タイマーとポモドーロ)
10. [統計を見る](#統計を見る)
11. [ゴミ箱](#ゴミ箱)
12. [バックアップと復元](#バックアップと復元)
13. [データの持ち方とお守り方](#データの持ち方とお守り方)
14. [よくある質問](#よくある質問)

---

## はじめての5分

1. **タスクを1つ追加する** — 上の入力欄に `明日レポート提出 30分 !2 #仕事` と入れて
   Enter。「期限・見積もり・優先度・タグ」を1行で解釈して登録します。
2. **完了にする** — タスク行の左のチェックボックスにチェックを入れると上に操作バーが
   出るので「{n} 件を完了」を押します。
3. **内容を編集する** — タスク名をクリックすると編集ダイアログが開きます
   （メモ・期限・時刻・リマインダー・優先度・リスト・見積もり・タグ）。
4. **探す** — 左の検索欄に入れると該当部分がハイライトされます。
5. **バックアップ** — 左下の⚙（ミルクアイコン）→「書き出し」で JSON を保存できます。

以上です。あとは自分の使い方に合わせて読み進めてください。

> **言語の切り替え**: ⚙ → 一番下の「言語」で 日本語 / English / 中文 を選べます。
> アプリのUI言語と入力言語は独立しています。UIが日本語でも `report tomorrow` と
> 英語で入力して構いません。

---

## タスクを追加する（クイック追加）

画面上部の入力欄に1行で入力して Enter。スペースがなくても語の中から解析します。

```
明日レポート提出 30分 !2 #仕事
```

| 書くもの | 書き方の例 |
|---|---|
| 期限 | `今日` `明日` `明後日` `金曜` `来週月曜` `9/15` `9月15日` `2026-10-01` |
| 期限（英/中） | `today` `tomorrow` `next fri` / `今天` `明天` `下周三` |
| 時刻 | `10時` `10時半` `午後3時` `9:30` `2pm` `10点` `下午3点` |
| 見積もり | `30分` `2時間` `1時間半` `45min` `1h30min` `0.5h` `2小时` `30分钟` |
| 優先度 | `!1`（高）`!2` `!3` |
| タグ | `#仕事` `#買い物`（複数可） |

- 「明日10時」のように日付と時刻は併用できます。日付なしで時刻だけ書くと「今日」の予定になります
- 追加先は**表示中のリスト**です。「すべて」や INBOX 表示中に追加したものは INBOX に入ります
- 解析できなかった語はタイトルにそのまま残るので、失敗しても情報は失われません
- **複数行の貼り付け**: 入力欄に改行入りのテキストを貼ると、1行 = 1タスクで一括登録
  できます（上限100行）。.txt / .md ファイルを画面にドロップしても同じです。
  メモ帳で書き溜めたTODOを流し込むのに便利です

### URL からのワンショット追加

`https://qnosuke.github.io/openmilk/?add=明日9時ゴミ出し` のようなURLを開くと、
その場で登録されます（10分以内の同じ行は再登録されません）。スマホのショートカットや
自動化から使えます。

---

## 完了・延期・削除

openmilk のチェックボックスは「完了ボタン」ではなく**選択**です。

1. 完了したいタスクのチェックボックスにチェックを入れる（複数可）
2. 上に出たバーで「{n} 件を完了」を押す
3. 選択をやめるときは「選択解除」

同じバーからできます:

- **延期**: 「{n} 件を延期」— 期限を1日ずらします（期限がないタスクは明日になります）
- **タグの一括追加・削除**: タグ名を入力して「+タグ」か「−タグ」。選択中の全タスクに
  反映されます

削除（ゴミ箱へ移動）はタスク名クリック→編集ダイアログの「削除」から行います。
誤って消してもゴミ箱から復元できます（[ゴミ箱](#ゴミ箱)）。

---

## 詳細を編集する

タスク名をクリックすると編集ダイアログが開きます。

- **メモ**: 複数行OK。一覧では1行目だけがプレビュー表示されます
- **期限・時刻**: 時刻ピッカーは5分刻みですが、直接入力（例 9:23）も保存できます
- **リマインダー**: 5分前〜1日前から選択。初回に設定すると通知の許可を求められます。
  通知はブラウザ/PWAを開いている間のみ届きます（サーバー通知はありません）
- **優先度**: `!1` が最優先。!1 のタスクは一覧で太字＋赤いラインで強調されます
- **リスト**: 移動先のリストを選択（空欄 = INBOX）
- **見積もり・実績**: 「1時間30分」のような入力も解析されます
- **サブタスク（分割）**: 下の「サブタスク」欄に1行ずつ入れて「分割して追加」。
  1行ごとにクイック追加の記法が使え、子タスクは親のリスト・タグを引き継ぎます。
  子は親の下にインデント表示され、親の行に「▸ 1/3」のような進捗が出ます。
  サブタスクは **1階層まで**です（子タスクの下にさらに孫は作れません）

ダイアログは「キャンセル」で閉じると変更を破棄し、「保存」・背景クリックで保存します。

---

## INBOX と仕分けモード

INBOX は思いついたことを**まず放り込む**場所。そして後で仕分ける場所です。

- INBOX を開くと**仕分けモード**になります（黒いバナーと、サイドバーの INBOX ボタンの
  色が反転しているのが目印）
- 各タスクの右側に `next action` / `waiting` / `someday` ボタンが出るので、
  クリックするだけでそのリストへ移動できます
- **キーボードでも仕分けできます**: `j`/`k`（または`↓`/`↑`）で行移動、`1`/`2`/`3` で
  next action/waiting/someday へ移動、`x` で完了、`e` で編集、`Space` で選択
  （`Esc` で解除）。全体では `a` で追加入力欄へ、`/` で検索欄へ跳べます
- INBOX の並び順は**追加順（古いものが先）で固定**。最初に入れたものから片付ける
  設計です（他のビューでの並び順変更は[こちら](#並び順)）

まずは考えるより書く。空になったら気持ちいい、がコンセプトです。

---

## リストを管理する

- 左サイドバーでリストを切り替えます。`すべて` / `INBOX` / 固定リスト
  （`next action` / `waiting` / `someday`）+ 自分で作ったリスト
- **新規作成**: ⚙ を開くと「新しいリスト」入力欄があります。作成するとそのリストに
  切り替わるので、そのままタスクを追加できます
- **削除**: リスト名の横の ×。**タスクは消えず、INBOX に戻ります**
- 固定リスト（next action / waiting / someday）は削除できません

---

## タスクを探す（検索・絞り込み）

- **検索**: 左の検索欄。タイトル・メモ・タグを対象に、一致した部分が
  黄色くハイライトされます
- **期間タイル**: サイドバーの「期限切れ / 今日 / 明日 / 1週間」の2×2タイル。
  押すとその期間で絞り込み、もう一度押すと解除できます
- **タグ絞り込み**: タスク行の `#仕事` チップ、またはサイドバーのタグクラウドを
  クリック。解除は上に出る「#仕事 ✕」ボタン
- **非表示タグ**: 見たくないタグは ⚙ のタグ一覧からミュート（⊘）できます。
  ミュートしたタグのついたタスクはすべてのビューから消えます（解除も⚙から）

---

## 並び順

- INBOX 以外のビューでは、右上の「並び順」で切り替えられます:
  **期限順**（近いものが先・時刻つきはその日の予定順）/**優先度順**/**追加順**
  （新しいものが先）。選択は保存されます
- INBOX は上のとおり追加順固定です
- 完了済みタスクは常にリストの最後にまとまります

---

## 計測タイマーとポモドーロ

- タスク行の **▶** で作業時間の計測開始、**⏸** で停止。計測できるのは同時に1タスクだけ
  （別のタスクで ▶ を押すと、前の計測は自動で停止して実績に加算されます）
- タスク完了時に計測中だった場合は、その時点までの経過が自動で実績に加算されます
- 計測中は経過がリアルタイムで表示され、見積もりを超えると赤くなります
  （「25m / 30m」のように見積もりと並べて出ます）

### ポモドーロ

計測中は自動でポモドーロが走ります:

- ヘッダーに 🍅 チップが現れ「1周目 あと24:59」のようにカウントダウンします
- 集中時間が終わると「休憩しましょう」の通知。休憩が終わると「集中を再開」の通知
- 4周ごとに休憩は長め（15分）になります
- 設定（集中 25分 / 休憩 5分 / 長い休憩 15分 / 長い休憩の間隔 4周）は ⚙ 内の
  「ポモドーロ」で変更できます

> 通知が届かないときは、ブラウザの通知権限を確認してください（⚙からではなく、
> リマインダーやポモドーロの初回設定時に許可を求められます）。

---

## 統計を見る

ヘッダー右上の「統計」タブで、完了タスクの実績を振り返れます。

- **今日 / 今週 / 今月 / 今年** の完了件数
- 見積もりの合計と実績（計測時間）の合計
- **予定通り率**: 見積もり付きタスクのうち、実績が見積もり以内に収まった割合

「見積もりが甘いな」と気づけるのが狙いです。見積もりはクイック追加で軽く付けるのがおすすめ。

---

## ゴミ箱

- 削除したタスクは**30日間**ゴミ箱に残ります（⚙ →「ゴミ箱」で開閉）
- ゴミ箱内でチェック →「{n} 件を復元」で元に戻せます
- 30日を過ぎたものは起動時に自動で完全削除されます

---

## バックアップと復元

⚙ →「書き出し」で全データ（削除済み含む）を1つの JSON ファイルに保存できます。

### 自動バックアップ（Chrome / Edge）

⚙ →「自動バックアップ」→「フォルダを選ぶ」で監視フォルダを指定すると:

- タスクに変更があるたび、フォルダ内の `openmilk-backup.json` に自動保存されます
- **起動時・フォーカス時に、このフォルダのバックアップから自動復元します**
  （同じタスクは新しい方が残るマージなので、上書きで消えることはありません）
- フォルダに `inbox.md` を置くと、アプリ起動時・フォーカス時に1行ずつタスクとして
  取り込みます（1行 = 1タスク。`- 牛乳を買う 明日 !1` のような箇条書きもOK。
  処理済みファイルは `imported/` フォルダへ移動します）
- `inbox.json`（下のバックアップ形式）を置いても同じく取り込まれます

**これが他環境との簡易同期になります**: フォルダを iCloud Drive や Dropbox に置き、
別のMacのopenmilkで同じフォルダを選べば、起動するだけでデータが揃います
（変更を取り込むタイミングは起動時とフォーカス時。削除も論理削除ごと同期されます）。

エージェント（OpenClaw など）はこのフォルダを介して「inbox.md に書く = タスク追加、
openmilk-backup.json を読む = 一覧取得」ができます。データ形式の正本は
`openmilk.schema.json`（https://qnosuke.github.io/openmilk/openmilk.schema.json）で公開しています。

### 書き出しと読み込み

- **復元・引っ越し**: 「読み込み」で JSON を投入。同じタスクは**新しい方**が残る
  冪等マージなので、空の状態からの復元でも、既存データとの統合でも安全です
- **おすすめの習慣**: 週1回など、書き出しファイルを Dropbox や iCloud に置いておくと
  安心です
- 完了済みタスクをまとめて消すには ⚙ →「完了タスクを削除」（2段階確認あり）。
  消す前に書き出しておくとより安心です

---

## データの持ち方とお守り方

openmilk の基本精神は **own your data（データは自分で持つ）** です。

- データはブラウザ内（IndexedDB）だけにあります。クラウドにもサーバーにも送られません
- **サイトデータを消すとタスクも消えます**: ブラウザの「サイトデータの削除」で
  openmilk のデータも消えるので、定期的な書き出しをおすすめします
- **PWAとして使う**: https://qnosuke.github.io/openmilk/ を Chrome / Safari で開いて
  「インストール」すると、アプリとして起動でき、オフラインでも動きます
  （開発用サーバー localhost:5173 ではオフライン動作しません）
- 複数デバイス間の自動同期は未実装です。持ち運ぶには「書き出し → 別デバイスで読み込み」
  のJSON経由で

---

## よくある質問

**Q. データはどこに保存されていますか？**
お使いのブラウザ内の IndexedDB です。このリポジトリにも、サーバーにも保存されません。

**Q. スマホと PC で同じデータを使えますか？**
自動同期は未実装です。書き出しJSONを受け渡すことで手動で同じデータにできます。

**Q. 完了ボタンが行に見当たりません**
チェックボックスで選んでから、上のバーの「{n} 件を完了」が仕様です。

**Q. 通知が来ません**
ブラウザを開いている間しか届きません（サーバー通知なし）。加えてOS/ブラウザの
通知権限が必要です。PWAとしてインストールしておくのがおすすめです。

**Q. 追加したはずのタスクが見つかりません**
表示中のリストに入っています。INBOX か「すべて」で確認し、非表示タグが
ついていないかも確認してください。

**Q. 間違えて同じタスクを何度も追加してしまいました**
URL の `?add=` は10分以内の再実行を自動でスキップします。手入力で重ねてしまった場合は
編集ダイアログの「削除」でゴミ箱へ。

---

設計とロードマップの詳細は [DEVELOPMENT_FLOW.md](./DEVELOPMENT_FLOW.md)（日本語）、
開発者・エージェント向けの情報は [AGENTS.md](./AGENTS.md)（日本語）と
[README.md](./README.md) を参照してください。
